// perfume-backend/utils/fileStorage.js

const deleteFile = (filePath) => {
    console.log(`[Stub] Удаление файла: ${filePath}`);
    // В будущем здесь будет логика удаления картинок
};

module.exports = { deleteFile };